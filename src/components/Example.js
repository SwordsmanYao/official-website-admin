import React from 'react';
import { Button } from 'antd';

const Example = () => {
  return (
    <div>
      <Button>test</Button>
      Example
    </div>
  );
};

Example.propTypes = {
};

export default Example;
